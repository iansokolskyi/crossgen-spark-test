{
  "name": "obsidian-spark",
  "version": "0.1.0",
  "description": "Spark Assistant - AI-powered automation for Obsidian",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "npm run lint:fix && npm run format && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check src/**/*.ts",
    "check": "npm run format:check && npm run lint && tsc -noEmit -skipLibCheck"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "ai",
    "automation"
  ],
  "author": "Ian",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@typescript-eslint/eslint-plugin": "^8.46.0",
    "@typescript-eslint/parser": "^8.46.0",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.24.2",
    "eslint": "^9.37.0",
    "eslint-config-prettier": "^10.1.8",
    "obsidian": "^1.7.2",
    "prettier": "^3.6.2",
    "typescript": "^5.7.3"
  }
}